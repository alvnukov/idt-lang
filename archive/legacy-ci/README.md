# Legacy CI Archive

This directory preserves pre-v8 compatibility/status workflows that are no
longer active proof-authority CI.

The current active CI entry point is `.github/workflows/v8-lean-status.yml`.
Archived workflows may still be useful for historical compatibility checks or
manual diagnostics, but they must not be treated as the Lean + IDT v8 proof
source of truth.
