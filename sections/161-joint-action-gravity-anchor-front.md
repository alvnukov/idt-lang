## 161. Joint Action-Gravity Anchor Front

Status:

`joint_action_gravity_anchor_front_initialized`

This is the next live front after calibrated QM closure.

The question is whether the two calibrated constants:

1. `calibrated_hbar_I`;
2. `local_G_anchor_I`;

are shadows of one deeper primitive structure.

The current answer is:

`possible_but_not_derived`.

### 161.1. Required intersection

The intersection cannot be the dimensional Planck combination alone:

\[
\ell_P=\sqrt{\frac{\hbar G}{c^3}}.
\]

That only proves that the units can be combined.

The theory needs a shared primitive object that enters both channels:

1. the QM action/phase channel;
2. the gravity source/clock-strain channel.

The candidate front is:

\[
W_{0,I},\tau_{0,I},m_{0,I},q_{\Phi,I}
\Rightarrow
A_{0,I}
\quad\text{and}\quad
\kappa_{\chi,I},G_I.
\]

Where:

1. \(W_{0,I}\tau_{0,I}\) must lock the action standard feeding `hbar_I`;
2. the same work/mass/source-response structure must lock the source
   normalization and clock-vacuum stiffness feeding `G_I`.

Status:

`shared_primitive_required_not_planck_units`

### 161.2. Machine target

The manifest now registers:

`joint_action_gravity_anchor_I = target`.

It is not accepted as:

1. `derived`;
2. `derived_conditional`.

until the joint route includes:

1. primitive tick closure;
2. primitive work closure;
3. primitive mass-anchor closure;
4. source-response charge closure;
5. action-standard closure;
6. first-principles `hbar_I`;
7. source-stress packet closure;
8. clock-strain source law;
9. clock-vacuum stiffness closure from source charge;
10. first-principles `G_I`.

Status:

`joint_action_gravity_anchor_guard_registered`

### 161.3. Forbidden shortcut

The route may not use:

1. `hbar_obs`;
2. `G_N`;
3. Planck units;
4. `calibrated_hbar_I`;
5. `calibrated_G_anchor_I`;
6. `local_G_anchor_I`.

Those are validation or our-universe anchors.

They can check a completed route, but they cannot create it.

Status:

`calibrated_anchor_shortcut_forbidden`

### 161.4. Success criterion

The front succeeds only if one primitive work/mass/source-response structure
does both jobs:

\[
\text{phase/action unit}
\quad\text{and}\quad
\text{source-to-clock-strain stiffness}.
\]

Then the theory would have a real quantum-gravity contact point:

`same_anchor_feeds_hbar_and_G`.

Until then, the honest status remains:

`two_calibrated_sectors_not_unified`.

### 161.5. Next concrete move

The highest-value move is W1+W3 from the dual-anchor matrix:

1. derive an inertial mass anchor without spectroscopy, \(G_N\), \(\hbar\), or
   Planck units;
2. derive a source-response charge from the same packets;
3. prove active/passive/inertial equality;
4. only then compare the resulting source law against local \(G\).

Section 162 makes this subproblem explicit as:

`source_response_charge_closure_I = target`.

Failure condition:

If the mass/inertia anchor and source-response charge cannot be made identical
without importing calibrated constants, the current theory remains an effective
two-anchor reconstruction, not a unification.

Status:

`primitive_mass_source_response_route_selected`
