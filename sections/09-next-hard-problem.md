## 9. Next Hard Problem

The next hard problem:

> derive inherited vacuum response \(\mathcal P_{E,I},\mathcal P_{B,I}\Rightarrow Z_{0,I}\), so \(\alpha_{\mathrm{em},I}=Z_{0,I}/(2R_{K,I})\) becomes non-fitted.

The next target is no longer “fit Planck's constant”.

The v5.28 audit route adds domain/theorem controls and an experimental gate ledger.

Dimensional constants are bridge coefficients.

Physical checks must be dimensionless:

$$
\alpha_{\mathrm{em},I}
=
\frac{e_I^2}{4\pi\epsilon_{0,I}\hbar_I c_I}
$$

and:

$$
l_{P,I}
=
\sqrt{\frac{\hbar_I G_I}{c_I^3}}
$$

but Planck units are crossover scales, not by themselves derivations.

The action and AB phase bridges now reduce to:

$$
U_I(h\circ k)=U_I(h)U_I(k)
\Rightarrow
S_I(h\circ k)=S_I(h)+S_I(k)
$$

and:

$$
\text{local phase covariance}
\Rightarrow
A_I
\Rightarrow
\Delta\phi_{AB,I}.
$$

The electromagnetic weak-field bridge now reduces to:

$$
A_I
\Rightarrow
\mathcal F_A[A]
\Rightarrow
F_I=dA_I
\Rightarrow
\partial_\mu F_I^{\mu\nu}=g_I^2j_I^\nu.
$$

Symmetry and Maxwell field-cost do not fix \(g_I\).

The selected route is:

$$
\mathcal P_{E,I},\mathcal P_{B,I}
\Rightarrow
\epsilon_{0,I},\mu_{0,I}
\Rightarrow
Z_{0,I}
\Rightarrow
\alpha_{\mathrm{em},I}
$$

with the bridge identity:

$$
\alpha_{\mathrm{em},I}
=
\frac{Z_{0,I}}{2R_{K,I}}.
$$

The next primitive target is:

$$
\frac{\mathcal P_{B,I}}{\mathcal P_{E,I}}
\Rightarrow
Z_{0,I}
\Rightarrow
\alpha_{\mathrm{em},I}
$$

without per-experiment fitting.

The first non-fitted verification target:

$$
h_I^{\mathrm{spectroscopy}}
=
h_I^{\mathrm{interference}}
=
h_I^{\mathrm{phase}}
$$

and:

$$
\alpha_{\mathrm{em},I}^{\mathrm{spectroscopy}}
=
\alpha_{\mathrm{em},I}^{\mathrm{scattering}}
=
\alpha_{\mathrm{em},I}^{\mathrm{Josephson/Hall}}
$$

with no re-fit between routes.

The first selected closure loop is:

$$
\mathfrak s_I(\eta)
\Rightarrow
\Delta S_I
\Rightarrow
\Delta\phi_I
\Rightarrow
\text{neutral two-path interference}
$$

and:

$$
\mathfrak q_I(\eta)
\Rightarrow
\Delta\phi_{q,I}
\Rightarrow
\text{Aharonov-Bohm phase}
\Rightarrow
\alpha_{\mathrm{em},I}.
$$

Its first no-refit closure equation is:

$$
\frac{|q_I|\Phi_{0,I}^{AB}}{h_I}
\to
1.
$$

Secondary targets:

1. derive or reject primitive \(\mathcal P_{E,I},\mathcal P_{B,I}\);
2. derive or reject a non-fitted \(Z_{0,I}\);
3. test \(\alpha_{\mathrm{em},I}=Z_{0,I}/(2R_{K,I})\);
4. derive QED running as a coarse-graining flow or declare it as residual;
5. derive \(\gamma_{ab}\) from primitive apparatus coupling instead of treating it as packet input;
6. derive packet update rules for \(\Theta_{q,I}\) under controlled gauge apparatus changes;
7. derive microscopic link stiffness \(a_{EF}\), matter calibration, and event-density calibration \(\lambda_I\);
8. populate experimental ledger tolerances \(\epsilon_k\) from declared source tables;
9. require one \(\alpha_{\mathrm{em},I}\) across Coulomb, scattering, spectroscopy, Josephson, and Hall gates;
10. compute \(\Delta E_I\) for a minimal clock/spectral transition model;
11. close light bending / Shapiro via \(\Pi_I^{ij}\to0\Rightarrow\gamma_I^{\mathrm{PPN}}\to1\), not a fit.

---
