# 175. Born/S2 As No Hidden Context-Only Fact

Status:

```text
research_note
no_proof_upgrade
no_Born_claim
```

## 175.1. Narrowed Question

The current Born wall should not be framed primarily as:

```text
why |psi|^2?
```

The sharper IDT question is:

```text
why is stable primitive third-order facticization inadmissible?
```

If stable primitive \(I_3 \ne 0\) is possible, the current primitive base does
not force S2 and therefore does not force Born. If stable primitive
\(I_3 \ne 0\) is impossible for structural IDT reasons, then the route becomes:

```text
no hidden context-only fact
=> no stable primitive I3
=> S2
=> positive pairwise comparison geometry
=> quadratic actualization
=> Born-like finite context probability
```

This note records the live research route. It is not a proof of Born.

## 175.2. Pattern Match With Hidden Joint-Only Invariants

The real-Hilbert/rebit separator rejects a composite candidate because it
contains a stable joint-only invariant:

```text
globally distinguishable
locally product-invisible
not exhausted by product-context witnesses
```

A primitive third-order facticization witness has the analogous single-context
shape:

```text
whole-context distinguishable
singleton/pairwise-invisible
not exhausted by pairwise context witnesses
```

So the candidate unifying principle is:

```text
No stable fundamental fact may be visible only to an unreduced whole context
when all admissible subcontext witness routes fail to expose it.
```

Call this:

```text
no_hidden_context_only_fact
```

This is stronger than ordinary finite accounting and different from assuming
Born. It constrains what can become a primitive fact.

## 175.3. Five Perspectives On The Same Wall

### Witness Perspective

Primitive \(I_3 \ne 0\) means the triple carries a stable facticizable residue
not generated by singleton or pairwise witnesses.

Kill test:

```text
construct stable ternary witness with admissible singleton/pairwise witnesses
and no contradiction
```

Current status:

```text
countermodel survives unless no_hidden_context_only_fact is added or derived
```

### Coarse-Graining Perspective

If fact formation is independent of refinement order and admissible
coarse-graining, then a primitive triple-only residue is suspect: grouping
\((A+B)+C\) and \(A+(B+C)\) should not create different stable facts.

Kill test:

```text
ternary residue remains invariant under all admissible regroupings
without becoming a hidden context-only fact
```

Current status:

```text
bracketing equality alone is insufficient
```

The current Lean separator includes a counter-witness:

```text
left grouping total = right grouping total
primitive ternary residue > 0
```

So ordinary grouping associativity can carry a ternary residue through both
bracketings. This does not kill \(I_3\).

The stronger rule would have to be:

```text
proper subcontext witness exhaustion
```

meaning that a stable whole-context fact must be exhausted by admissible
proper-subcontext witness data. That rule would block primitive ternary
residue, but it is not yet independently derived.

### Product-Context Perspective

Product-context exhaustion already rejects hidden joint-only composite
invariants. The Born/S2 wall may require the one-context analogue:

```text
subcontext witness exhaustion rejects hidden whole-context invariants
```

Kill test:

```text
derive product-context exhaustion but fail the analogous readout-context
exhaustion rule
```

Current status:

```text
analogy strong, theorem absent
```

### Metricity Perspective

Hilbert carrier and spacetime metricity may be sibling projections of a lower
principle:

```text
stable distinctions are only pairwise comparison data
```

If true, S2 is not an arbitrary QM axiom; it is the readout shadow of
metricity in fact formation.

Kill test:

```text
stable distinguishability requires a non-pairwise primitive without becoming
a hidden context-only fact
```

Current status:

```text
promising but unproved
```

### Sector Perspective

Higher-order facticization may be logically possible outside the QM-scale
sector. Then Born is not universal across all possible sectors; it is the law
of the stable QM readout sector.

Kill test:

```text
find or construct a stable sector with reproducible no-refit I3 != 0
```

Current status:

```text
open sector boundary
```

## 175.4. Research Decision Point

The next theorem target is not `Born` directly.

The next target is:

```text
no_hidden_context_only_fact
```

If it can be derived from fundamental unknownness, admissible witness
generation, loss accounting, and context/refinement coherence, then S2 can be
derived without importing Born.

If it cannot be derived, then the honest options are:

```text
1. declare S2 as a falsifiable QM-sector law;
2. declare S2 as a new primitive;
3. allow non-QM sectors with stable primitive higher-order facticization.
```

Forbidden upgrade:

```text
do not mark Born or S2 as proved until no_hidden_context_only_fact has a
machine-checkable proof artifact or an explicit sector-law boundary.
```

## 175.5. Anti-Circularity Audit

`no_hidden_context_only_fact` is useful only if it is derived from a deeper
source.

It is not progress to define:

```text
no_hidden_context_only_fact := no stable primitive I3
```

That would merely rename S2.

The current Lean separator now checks the negative side:

```text
B2-like accounting does not force no_hidden_context_only_fact.
```

So the live route must supply an independent source, for example:

```text
stable facts must be subcontext-witness exhausted;
fundamental stable distinction is metric/pairwise comparison data;
hidden whole-context facts are forbidden by primitive unknownness itself.
```

If none of these can be proved, the honest conclusion is:

```text
S2 is a QM-sector law or primitive, not a derived theorem.
```

## 175.6. Broad Chain Hit After The Born Wall Pass

The broad pass changed the local status. The result is not:

```text
Born proved
```

The result is:

```text
finite dependency chain hit
universal Born/S2 still open
```

The currently executable chain is:

```text
compatible-kernel additivity
=> normalized bilinear overlap uniqueness
=> normalized orientation-overlap selector
=> canonical quadrature J
=> phase-bundle normalized-overlap carrier
=> finite complex-sector route
```

The checks are:

```text
scripts/evaluate_overlap_uniqueness.py
  normalized_bilinear_overlap: UNIQUE_HIT
  cubic_overlap: WEAK_AMBIGUITY
  tanh_overlap: WEAK_AMBIGUITY
  direct_cosine_projection: IMPORTED_HIT

scripts/evaluate_overlap_primitive_route.py
  normalized_orientation_overlap: NEW_PRIMITIVE_HIT, 8/8

scripts/evaluate_qm_compressed_route.py
  phase_bundle_normalized_overlap: CARRIER_ROUTE_HIT, 8/8
  real_normalized_overlap: NEW_WALL

scripts/verify_finite_qm_route.py
  finite_qm_route_gate=PASS checks=31
```

This is stronger than an isolated finite Born-angle fit. It says that, on the
current finite screens, the same non-imported route survives kernel
composition, overlap uniqueness, Bell/Born-angle behavior, phase-bundle
carrier behavior, local tomography, tensor multiplicativity, and negative
carrier controls.

## 175.7. What This Does Not Yet Prove

The remaining wall is now more precise. The open obligations are:

```text
derive_compatible_kernel_additivity_from_primitives
derive_normalized_overlap_selector_from_primitives
derive_phase_bundle_J_structure_from_primitives
generalize_overlap_selector_to_all_readout_contexts
proper_subcontext_pairwise_basis
second_order_facticization
positive_quadratic_actualization_principle
```

The key point is that probability normalization is no longer the right place
to search. Probability can normalize supplied weights, but it cannot select the
weight law. The live route must work below probability and prove why stable
facticizable readout is exhausted by normalized pairwise overlap.

## 175.8. Current Research Decision

The next serious target is:

```text
primitive unknownness
=> compatible kernel additivity
=> normalized overlap uniqueness
=> proper-subcontext/pairwise readout coverage
```

If this target is proved, the Born wall becomes a route-generalization problem
rather than a missing-selector problem.

If this target fails, the honest conclusion is narrower:

```text
normalized overlap is a powerful finite QM-sector law or boundary principle,
not yet a theorem from primitive unknownness.
```

This is still useful: it would identify the smallest currently visible
additional principle needed for finite QM behavior, without importing Hilbert,
Born, unitary dynamics, or a Bell table.

## 175.9. Primitive-Boundary Audit

The broad pass also checked whether the current B1 successor primitive base
already contains the missing inputs.

Current B1 result:

```text
B1 binds admissible atoms/roles to constructor-generated context, fact, route,
generator, and local-fact witnesses.
B1 preserves no-Hilbert/no-Born/no-unitary/no-tensor import guards.
B1 yields the internal full-QM obligation bundle.
```

Current B1 limitation:

```text
B1 does not yet derive compatible-kernel additivity.
B1 does not yet derive normalized overlap uniqueness.
B1 does not yet derive phase-bundle J.
B1 does not yet derive proper-subcontext/pairwise readout coverage.
```

The finite selectors say what the missing primitive-boundary theorem would have
to prove:

```text
scripts/evaluate_kernel_additivity_principle.py
  additive_kernel_composition: PRINCIPLE_HIT, 7/7
  imported_additive_sum: IMPORTED_HIT
  nonlinear/magnitude controls: REJECTED

scripts/evaluate_phase_bundle_j_derivation.py
  canonical_quadrature_J: J_DERIVATION_HIT, 6/6
  imported_complex_J: IMPORTED_HIT
  identity/half-speed/double-speed rotations: NEAR_MISS
  hyperbolic/shear transports: REJECTED
```

So the result is not "B1 proves Born". The result is sharper:

```text
B1 gives a bound primitive interface and import discipline.
The finite QM chain identifies the exact extra structure needed.
The missing theorem is a primitive-boundary theorem:
  context-first unknownness
  -> compatible additive scalar kernel
  -> normalized overlap uniqueness
  -> canonical quadrature J
  -> pairwise/proper-subcontext readout coverage
```

This is the current high-value route. It attacks Born below probability and
avoids the dead route where probability axioms merely normalize weights that
were already supplied.

## 175.10. Three Alternatives Versus Three-Dimensional Space

The "three alternatives" signal must not be confused with physical 3D space.

Three alternatives are important because they are the first arity where a
non-pairwise primitive residue can appear:

```text
one-body weights
pairwise comparison/interference
primitive ternary residue
```

Physical three-dimensional geometry is a different object. It can be built from
pairwise metric data; it does not require a primitive third-order fact.

Therefore the disciplined statement is:

```text
the third alternative is the first test that separates metric/pairwise worlds
from higher-order context-only worlds.
```

Possible bridge to geometry:

```text
Born/Hilbert readout and spacetime metricity may both be projections of the
same lower rule: stable distinguishability is pairwise comparison data.
```

This remains a hypothesis. It is a route to test, not evidence by analogy.

## 175.11. Broad-Pass Result: What Does Not Help Enough

The wider pass rules out several tempting shortcuts.

### NUSD / Pointwise Witnessability

NUSD says stable distinctions need an admissible route to facticization or loss
accounting. This is below Born and remains useful.

But it does not prove S2:

```text
singleton routes exist
pairwise routes exist
ternary route exists
stable I3 != 0
```

This satisfies pointwise witnessability. The ternary fact is not unwitnessed;
it is witnessed by a ternary route. So NUSD blocks hidden unwitnessed labels,
not witnessed higher-order facticization.

Required strengthening:

```text
proper subcontext witness exhaustion
```

### Finite Route Compactness

Finite route compactness can make witness families bounded or finitely
generated. It still does not imply S2 unless the finite basis is forced to be
singleton/pairwise.

Counter-shape:

```text
finite basis = singleton witnesses + pairwise witnesses + one ternary witness
```

This is compact but not Born-like.

Required strengthening:

```text
pairwise/proper-subcontext basis theorem
```

### Bare Holonomy / Bell / Gravity Pattern

The Hilbert/Bell/gravity common pattern remains important:

```text
failed global table
context-cycle residue
witness discipline
metric pressure
```

But this common pattern is broader than QM. A finite \(Z_2\) holonomy
countermodel can have obstruction and context residue without Hilbert, Born,
or S2.

Required strengthening:

```text
obstruction residue must be representable as pairwise metric comparison data
```

### Metricity

Metricity is now the cleanest surviving route, but it is dangerous.

If stated as:

```text
stable distinguishability is pairwise comparison data
```

then S2 follows almost immediately.

But this is only real progress if metricity is derived from a lower principle,
not assumed because it gives Born.

The remaining honest target is:

```text
fundamental unknownness
+ admissible witness generation
+ no primitive global fact table
+ loss/accounting discipline
=> stable distinguishability is pairwise metric comparison data
```

If this theorem fails, IDT should not pretend that Born is derived. The fallback
is:

```text
S2 is a falsifiable QM-sector law.
```

## 175.12. Primitive-Base Stress Test

The direct primitive-base route also fails in its current form.

Current base discipline contains:

```text
constructor-generated witnesses;
no primitive global fact table;
local readout witnesses;
loss/accounting routes.
```

This does not prohibit:

```text
a constructor-generated local ternary witness
that is not a global fact table
and has an accounting route.
```

The Lean separator now records this explicitly:

```text
current primitive discipline
does not force
pairwise/proper-subcontext basis
```

This matters because it prevents a false proof:

```text
no global fact table
=> no I3
```

That implication is invalid. A primitive \(I_3\) witness can be local to one
readout context and still satisfy no-global-table discipline.

The only remaining theorem target is therefore sharper:

```text
fundamental unknownness
+ admissible witness generation
+ no primitive global fact table
+ loss/accounting discipline
+ ?
=> proper_subcontext_pairwise_basis
```

The missing `?` cannot be ordinary witnessability, finite compactness,
associativity, or bare holonomy. The current candidates are:

```text
minimal arity of fundamental stable distinction;
hereditary localizability of facticization across proper subcontexts;
metricity of stable distinguishability derived from non-globality;
explicit QM-sector S2 law.
```

If none of the first three can be derived, the theory must mark Born as
conditional on a sector law rather than as a primitive-base theorem.

## 175.13. Probability Audit

The probability layer is not the Born selector.

Current operational probability is:

```text
context probability = normalized supplied readout weight
```

This is the correct readout-facing object, but it is too high-level to derive
Born. It assumes that a weight has already been selected.

The Lean audit now checks the negative side:

```text
linear positive weights normalize;
quadratic positive weights normalize;
cubic positive weights normalize.
```

Therefore:

```text
normalization
+ positivity
+ context total
+ stable frequency interpretation
does not select the quadratic law.
```

Stable frequencies only estimate the probabilities produced by a readout
context. They do not explain why the readout weight has exponent two rather
than one, three, or a general higher-order context functional.

So the lower layer must be:

```text
facticization weight selector
```

not:

```text
probability axiom
```

The current dependency should be read as:

```text
proper_subcontext_pairwise_basis
=> S2
=> positive comparison geometry
=> quadratic facticization weight
=> normalized context probability
=> stable experimental frequency
```

Forbidden shortcut:

```text
do not use probability normalization, frequency, or Kolmogorov-style finite
context axioms as a proof of Born.
```

## 175.14. Positive Signal: Normalized Orientation Overlap

The broad pass found one nontrivial positive route that should not be ignored:

```text
normalized_orientation_overlap
```

This is not a probability axiom. It is a candidate selector below probability:

```text
unexposed distinctions carry reversible orientation constraints;
context transport composes by norm-preserving rotations;
local readout exposes normalized bilinear overlap;
metric bottlenecks attenuate overlap multiplicatively;
no direct Born/cosine table is imported.
```

Existing evaluator:

```text
scripts/evaluate_overlap_primitive_route.py
```

Current result:

```text
route = normalized_orientation_overlap
verdict = NEW_PRIMITIVE_HIT
passed = 8/8
imports = none
open_parameters = none
```

Passed screens include:

```text
no-signalling;
reversible context composition;
CHSH/Tsirelson boundary;
full singlet/Born angle curve on the checked grid;
metric composition;
import screen;
parameter closure;
primitive minimality.
```

This matters because it supplies a concrete candidate for the missing layer:

```text
facticization-weight selector = normalized bilinear overlap of transported
orientation constraints
```

It also explains why probability was the wrong layer to attack:

```text
probability normalizes selected weights;
normalized overlap selects the pairwise weight/correlation form.
```

Boundary:

```text
This is not a universal Born/S2 proof.
```

Remaining obligations:

```text
1. derive reversible orientation transport from the primitive base;
2. derive normalized bilinear overlap as the unique exposed invariant;
3. generalize from the finite Bell/angle screen to arbitrary admissible
   readout contexts;
4. prove that no stable primitive ternary facticization survives outside this
   overlap-covered sector, or declare the result sector-limited.
```

Updated live route:

```text
fundamental unknownness
+ admissible context transport
+ orientation preservation
+ normalized overlap exposure
=> pairwise facticization-weight selector
=> S2 in the covered sector
=> Born-like context probability after normalization
```

The current best interpretation is:

```text
normalized overlap is the first serious finite hit for the weight-selector
layer, not a completed derivation of Born.
```

## 175.15. Primitive-Boundary Candidate Classifier

The next broad pass classifies the possible boundary packages instead of
arguing from one path at a time.

New executable artifact:

```text
Proofs/QMClosure/PrimitiveBoundaryQMChain.lean
scripts/evaluate_primitive_boundary_qm_chain.py
```

The classifier separates:

```text
b1_bound_interface_only
  B1 constructor binding + import guards
  not enough for finite Born chain

b1_plus_compatible_kernel_only
  additive kernel without overlap/J/pairwise coverage
  partial boundary only

b1_plus_kernel_overlap_j_without_pairwise_coverage
  finite-chain inputs without hidden-ternary exclusion
  still not enough for universal S2/Born

full_primitive_boundary_candidate
  B1 binding
  + no target imports
  + compatible kernel additivity
  + normalized overlap uniqueness
  + phase-bundle J
  + proper-subcontext/pairwise coverage
  + no hidden ternary context-only fact

qm_sector_overlap_boundary
  same package, but explicitly sector-limited

hilbert_born_import_control
  rejected as imported target structure

hidden_ternary_context_control
  rejects the shortcut where overlap/J exist but primitive ternary facts remain

drop-one controls
  remove exactly one of the five required principles
  each fails the primitive-boundary gate
```

The current outcome is:

```text
primitive_boundary_qm_chain =
  PRIMITIVE_BOUNDARY_CANDIDATE_IDENTIFIED_NOT_DERIVED
```

Meaning:

```text
There is now a precise candidate package that would close the checked finite
Born/phase-bundle chain without target imports.

But the package is not yet derived from primitive unknownness.
```

This is progress because the wall is no longer diffuse. The remaining task is
not "prove Born somehow"; it is:

```text
derive or explicitly sector-bound the five principles:
  compatible_kernel_additivity
  normalized_overlap_uniqueness
  phase_bundle_J
  proper_subcontext_pairwise_coverage
  no_hidden_ternary_context_fact
```

The drop-one controls make this minimal in the current gate: removing any one
of the five principles breaks either the finite-chain inputs, the pairwise/S2
coverage, or the final projection into the checked finite QM route.

If all five are derived from context-first unknownness, the finite Born wall is
converted into a generalization/formalization problem. If they cannot be
derived, then the minimal honest boundary is a QM-sector law built around
normalized overlap and pairwise facticization coverage.

## 175.16. Parked Hidden Unit-Factor Born Hypothesis

The user-supplied hypothesis is important:

```text
Maybe Born has a deeper form with an extra factor that is equal to one in the
ordinary quantum sector.
```

This route is parked for now. It is not part of the active Born proof-search
frontier while the direct Born route through signed overlap is being pursued.

The disciplined IDT form is:

```text
p_i = |a_i|^2 chi_i / sum_j |a_j|^2 chi_j
```

where:

```text
chi_i = 1
```

on ordinary finite QM fixtures.

New executable screen:

```text
scripts/evaluate_born_hidden_factor_screen.py
```

The screen separates four cases:

```text
global_context_unit_factor
  chi_i = chi(C) for every outcome in the context
  cancels under normalization
  gauge-equivalent inside the probability readout

outcome_bias_unit_in_qm / product_factor_unit_in_qm
  nontrivial outside chi=1
  but breaks coarse-graining and product multiplicativity on the current screen

ternary_residue_unit_in_qm
  nontrivial only at three alternatives
  becomes a hidden ternary context-only factor
  hits the same S2/I3 wall

declared_qm_unit_factor
  compatible with all current QM fixtures
  but has no predictive off-fixture law
  therefore remains an open sector-factor boundary, not a proof
```

Conclusion:

```text
A hidden factor is possible only in two honest ways:
1. gauge factor: it cancels and has no observable probability content;
2. sector factor: it is exactly one in ordinary QM but becomes nontrivial only
   when a new non-QM variable is active.
```

This may be the right bridge to gravity/scale effects: ordinary QM gates freeze
clock/source response, so the factor reads as one. But the factor cannot be an
arbitrary outcome correction. To survive, it must preserve:

```text
normalization
repeatability
coarse-graining
product multiplicativity
no hidden ternary context-only residue
no target Born/Hilbert import
```

So the useful next target is not:

```text
change Born by hand
```

but:

```text
derive a sector factor chi from context/inheritance/clock-source response,
prove chi=1 on frozen finite QM contexts, and find the first admissible
off-QM regime where chi != 1 without breaking the gates above.
```

## 175.17. Direct Born Search: Affine Readout Of Signed Overlap

Returning to the direct Born route, the sharper target is:

```text
normalized overlap is a signed binary expectation r
```

not:

```text
probability is guessed as |a|^2
```

For a binary readout, if the two outcomes are complements, then the signed
expectation relation is:

```text
r = p(+) - p(-)
p(+) + p(-) = 1
```

so:

```text
p(+) = (1 + r) / 2
```

The phase-bundle double cover then gives:

```text
r = 2 |a|^2 - 1
```

therefore:

```text
p(+) = |a|^2
```

New executable screen:

```text
scripts/evaluate_born_from_overlap_affine_readout.py
```

It tests candidate binary selectors over signed normalized overlap:

```text
affine_born_from_signed_overlap
cubic_expectation_deformation
quintic_expectation_deformation
tanh_expectation_deformation
threshold_selector
absolute_overlap_selector
imported_amplitude_square_control
```

Required gates:

```text
bounded probability;
repeatability/exclusion;
complement symmetry;
unbiased zero overlap;
affine mixture response;
phase-bundle square reduction;
no Born/amplitude-square import.
```

Current result:

```text
affine_born_from_signed_overlap:
  AFFINE_BORN_SELECTOR_HIT

imported_amplitude_square_control:
  IMPORTED_HIT

nonlinear/threshold/unsigned controls:
  fail affine mixture, complement, endpoint, or phase-bundle-square gates
```

This is a stronger direct Born route than probability normalization:

```text
normalized overlap
+ signed expectation semantics
+ affine mixture response
+ phase-bundle double cover
=> Born square
```

Remaining proof obligation:

```text
derive signed expectation semantics and affine mixture response from the
primitive-boundary package.
```

If that derivation succeeds, the positive quadratic actualization principle is
not an extra Born primitive; it is the probability readout of normalized signed
overlap through a phase-bundle double cover.

## 175.18. Affine Readout Is Below Born

The affine step must not be smuggled in as a probability axiom. It can be
screened below Born as an operational randomization principle:

```text
externally choose branch A with frequency lambda;
externally choose branch B with frequency 1-lambda;
then the observed frequency of a facticizable binary record is
lambda p_A + (1-lambda) p_B.
```

New executable screen:

```text
scripts/evaluate_affine_readout_principle.py
```

Candidates:

```text
operational_frequency_affine_mix
imported_convex_probability_control
quadratic_frequency_mix
odds_logit_mix
max_frequency_mix
min_frequency_mix
```

Required gates:

```text
bounded readout;
endpoint recovery;
branch-label symmetry;
frequency calibration;
randomizer-tree refinement associativity;
no convex probability axiom import.
```

Current result:

```text
operational_frequency_affine_mix:
  AFFINE_READOUT_PRINCIPLE_HIT

imported_convex_probability_control:
  IMPORTED_HIT

nonlinear controls:
  fail frequency calibration or endpoint/refinement behavior
```

This makes the Born route more concrete:

```text
external randomization/frequency calibration
=> affine readout over signed expectation
normalized signed overlap + phase-bundle double cover
=> p = |a|^2
```

Remaining gap:

```text
prove that the primitive readout language admits this external-randomization
calibration for facticizable records.
```

That is lower than Born and does not require Hilbert space. It is now one of
the best candidates for the missing positive quadratic actualization route.

## 175.19. Direct Finite Born One-Pass Gate

The direct Born route is now checked as one composed gate:

```text
scripts/evaluate_born_direct_one_pass.py
```

It composes:

```text
primitive-boundary candidate;
normalized overlap uniqueness;
phase-bundle J;
operational randomization -> affine readout;
signed overlap affine readout -> |a|^2;
negative/import controls.
```

Current verdict:

```text
DIRECT_FINITE_BORN_ROUTE_HIT_NOT_UNIVERSAL_PROOF
```

Meaning:

```text
The finite direct Born selector route closes without target imports.
It still does not prove universal Born from primitives.
```

The remaining universal obligations are now minimal and explicit:

```text
derive_full_primitive_boundary_package_from_context_first_unknownness
derive_external_randomization_frequency_calibration_for_facticizable_records
derive_signed_expectation_readout_for_normalized_overlap
derive_phase_bundle_double_cover_for_all_admissible_readout_contexts
generalize_from_finite_screens_to_universal_Born_contexts
```

This is the strongest current Born status:

```text
finite direct Born route hit
universal Born proof open
```

The old wall "why square?" is no longer opaque on the finite route. It reduces
to:

```text
why normalized signed overlap is the primitive readout expectation;
why external randomization calibrates facticizable frequencies affinely;
why the phase bundle gives the double-cover relation r = 2|a|^2 - 1.
```

## 175.20. Direct Finite-QM One-Pass Gate

Born is not enough by itself. The next gate checks whether the current finite
QM route remains coherent after replacing the old Born wall with the direct
finite Born selector:

```text
scripts/evaluate_qm_direct_one_pass.py
```

It composes:

```text
finite QM route gate;
direct finite Born one-pass gate;
FPD/projective residual closure;
complex-Hilbert representation route candidate;
unitary/generator dynamics route candidate;
general composite/tensor route candidate;
calibrated phase-scale boundary;
negative import controls for Hilbert, tensor, unitary, convex probability, and hbar.
```

Current verdict:

```text
DIRECT_FINITE_QM_ROUTE_HIT_UNIVERSAL_QM_OPEN
```

Meaning:

```text
The direct finite-QM route is internally coherent and Born is no longer the
active finite-route blocker. This is still not a universal proof of QM.
```

The remaining blockers moved to the universal layer:

```text
derive the full primitive-boundary package from context-first unknownness;
derive affine frequency calibration for facticizable records;
derive signed expectation readout and phase-bundle double cover generally;
derive spectrality and rich reversible symmetry;
derive continuous reversible dynamics and generator closure;
derive arbitrary composite/entanglement/projective-limit closure;
derive NUSD plus conservative projective gluing from primitives;
keep hbar_I calibrated unless a new scale primitive is proved;
generalize finite screens to all admissible QM contexts.
```

This is a real advance, but not a claim upgrade to full QM. The strongest
current status is:

```text
finite direct-QM route hit
universal QM proof open
```

## 175.21. CGSC/QM One-Pass Closure Status

The broader CGSC/QM one-pass gate now recognizes the stronger primitive result:

```text
CGSC_DERIVED_FROM_PRIMITIVES
```

and no longer treats it as the older weaker status:

```text
SUCCESSOR_BASE_DERIVATION_REGISTERED
```

Updated executable checks:

```text
scripts/evaluate_cgsc_extension_wall_probe.py
scripts/evaluate_cgsc_primitive_bridge.py
scripts/evaluate_cgsc_qm_one_pass_closure.py
```

Current one-pass verdict:

```text
STRUCTURAL_ROUTE_READY_FORMALIZATION_WALL
```

Observed package status:

```text
global_failed = 0
package_evidenced = 3
package_blocked = 0
finite_gate_failures = 0
open_residuals = 0
draft_checks_failed = 0
```

Meaning:

```text
The current broad route is not structurally blocked at the CGSC package level.
The remaining wall is formalization: turn the package projections into semantic
target proofs for Hilbert/Born/unitary/tensor QM and replace conditional
packages with machine-checkable formal artifacts.
```

This is not a full-QM proof. It is the strongest current closure status:

```text
CGSC primitive route derived
finite direct Born/QM route hit
CGSC/QM structural route ready
formal full-QM proof still open
```

## 175.22. Born/Hilbert/Bell Primitive-Link Pass

The next broad pass treats Born, Hilbert, and Bell as one primitive readout
problem rather than three independent targets.

Hypothesis:

```text
Bell/contextual obstruction,
Born readout,
and Hilbert carrier pressure

are projections of one lower object:

normalized oriented distinguishability
with finite facticizable readout witnesses.
```

Executable pass:

```text
scripts/evaluate_born_hilbert_bell_primitive_link.py
```

The route decomposes the three targets as:

```text
Bell:
  no primitive global fact table;
  context-indexed orientation overlap;
  bounded no-signalling correlation readout.

Born:
  normalized orientation overlap;
  signed binary expectation;
  affine facticizable frequency readout;
  phase-bundle double cover;
  p = |a|^2 on the finite route.

Hilbert:
  phase-bundle carrier pressure;
  local tomography/product-context exhaustion;
  spectral exposed contexts;
  rich D_cl reversible symmetry;
  bounded-composite separator screens.
```

Current verdict:

```text
BORN_HILBERT_BELL_PRIMITIVE_LINK_CONDITIONAL_HIT_UNIVERSAL_UNIQUENESS_OPEN
```

Meaning:

```text
The finite graph supports a shared primitive link:
orientation overlap explains the Bell angle screen;
signed/affine readout explains the finite Born square;
phase-bundle/local-tomography screens create complex-Hilbert pressure and reject
real-Hilbert, boxworld, generic GPT, and finite residual controls.
```

This is stronger than treating Born/Hilbert/Bell separately. It suggests the
right primitive object is not probability, Hilbert vector, or Bell table, but:

```text
finite oriented distinguishability read out through admissible facticization.
```

Common-nature interpretation:

```text
Bell is the obstruction/readout projection:
  no primitive global fact table, only context-indexed oriented correlations.

Born is the frequency/readout projection:
  signed oriented overlap becomes affine facticizable probability.

Hilbert is the carrier/representation projection:
  the same oriented distinguishability needs a minimal faithful phase-bundle
  representation stable under composition, tomography, and reversible routes.
```

So the shared object is not yet a named physical substance. It is a stricter
primitive pattern:

```text
context-local oriented distinguishability
+ finite witness/facticization discipline
+ admissible composition/reversibility
```

The remaining wall is also sharper:

```text
derive S2 or a QM-sector S2 law from context-first unknownness;
derive signed expectation readout generally;
derive affine frequency calibration for facticizable records;
derive phase-bundle double cover generally;
derive spectral decomposition and rich reversible symmetry from D_cl;
derive recoverable oriented filtering without importing purification;
prove universal complex-Hilbert uniqueness or exhibit a countermodel.
```

This does not prove full QM. It says the same primitive link now carries all
three fronts as a conditional route, while universal complex-Hilbert uniqueness
and universal Born remain open.

## 175.23. Born/Hilbert Result With Bell As Control

Bell is no longer the active wall in this pass. The normalized-orientation
overlap route already closes the finite Bell/CHSH/Tsirelson readout screen
without importing a Bell table, while the direct cosine route remains classified
as an import control.

The active Born/Hilbert result is:

```text
Born:
  finite selector hit;
  universal primitive derivation open.

Hilbert:
  finite complex-like carrier hit;
  real/quaternionic/boxworld/generic controls rejected;
  universal carrier uniqueness open.
```

Executable status:

```text
scripts/evaluate_born_hilbert_bell_primitive_link.py

BORN_STATUS:
  finite Born readout is selected once normalized signed overlap, affine
  facticizable frequency readout, context normalization,
  exclusivity/coarse-graining, operational equivalence, and phase-bundle double
  cover are supplied.

BORN_BLOCKER:
  derive positive quadratic actualization, signed expectation readout, affine
  frequency calibration, and phase-bundle double cover from B1 or a successor
  primitive base; otherwise Born remains a conditional sector law.

HILBERT_STATUS:
  complex-Hilbert-like finite sector survives all current separator screens,
  while real/quaternionic/boxworld/generic controls are rejected.

HILBERT_BLOCKER:
  prove that every admissible fundamental finite carrier has a constructive
  phase-bundle/local-tomography/spectral-symmetry witness, or exhibit a concrete
  non-Hilbert constructive carrier countermodel.
```

So the current result is not a failure to explain Born/Hilbert. It is a precise
lowering of the wall:

```text
Born wall:
  not "why square after assuming Hilbert";
  but "why positive quadratic actualization is forced by primitive
  facticization/readout".

Hilbert wall:
  not "why complex Hilbert survives the finite screens";
  but "why every admissible fundamental carrier must be constructively
  generated by the phase-bundle/tomography/spectral-symmetry route".
```

The next primitive-base decision is therefore narrow. Either B1 must derive
constructive actualization and constructive carrier witness completeness, or a
successor primitive base must include a stricter witness-completeness principle.
Adding Born probability or Hilbert space directly would be circular; adding a
constructive witness-completeness principle may be legitimate because it speaks
below probability and below carrier representation.

## 175.24. Concrete Born/Hilbert Sector-Projection Result

The non-intermediate result of this pass is now machine-checkable:

```text
Proofs/QMClosure/ConstructiveWitnessPrimitiveBase.lean
```

It introduces a B2-style successor contract:

```text
B2ConstructiveWitnessBase
```

The contract is intentionally below Born and below Hilbert. It does not contain
probability amplitudes, projectors, Hilbert vectors, tensor products, or a Bell
table. It also does not assert that empirical QM formulas are exact at all
scales. Born/Hilbert are used as orientation targets for the standard finite
QM-sector projection, while beyond-QM correction channels remain open.

The B2 contract contains only generated witness-completeness obligations:

```text
primitive-boundary closure;
signed-overlap readout witnesses;
affine facticizable frequency witnesses;
phase-bundle double-cover witnesses;
phase-bundle carrier screen;
local tomography;
spectral exposed contexts;
rich D_cl reversible symmetry;
constructive carrier witness;
no hidden joint-only generation;
no Born/Hilbert/tensor imports.
```

Lean theorem:

```text
b2_constructive_witness_base_closes_born_hilbert_finite_routes
```

Statement-level meaning:

```text
If a B2 constructive-witness base is ready, then:

1. DirectFiniteBornRouteHit holds.
2. PositiveQuadraticActualizationFromSignedOverlap holds.
3. FiniteHilbertCarrierRouteHit holds.
```

Concrete current finite instance:

```text
current_finite_b2_constructive_witness_base_ready
current_finite_b2_closes_born_hilbert_finite_routes
```

Non-overclaim guard:

```text
current_finite_b2_still_not_universal_qm
```

This is the first concrete sector-projection result for the Born/Hilbert wall:

```text
B1 constructor binding + import guards:
  insufficient.

B2 constructive witness completeness:
  sufficient for current finite Born/Hilbert standard-QM sector projection.

Universal QM:
  still open.

Exact finality of empirical QM formulas:
  not claimed.

Beyond-QM correction channel:
  open unless independently ruled out.
```

The next scientific question is no longer "what is the Born/Hilbert blocker?"
The blocker is identified. The question is:

```text
Can constructive witness completeness itself be derived from context-first
fundamental unknownness, or must it be accepted as the B2 primitive principle?
```

The guard added in Lean is:

```text
current_b2_is_standard_qm_sector_projection_not_final_physics
```

That is the discipline needed here: do not fit primitives to Born/Hilbert as
final truth. Use Born/Hilbert to identify the stable observed sector, then keep
the lower theory free to produce audited deviations outside that sector.

## 175.25. Audited Beyond-QM Correction Channel

The correction direction is now explicit in Lean:

```text
BeyondQMCorrectionChannel
AuditedBeyondQMCorrectionChannel
```

The channel is not a free residual term. A correction is admissible only if:

```text
standard QM-sector projection is held fixed;
lower primitive source is declared;
scale or context boundary is declared before comparison;
correction observable is declared;
no post-fit retuning is allowed;
holdout comparison is required;
Born/Hilbert readout is not redefined after seeing data;
the correction reduces to the standard QM sector inside its boundary;
the correction may be zero.
```

Negative control:

```text
free_fit_correction_channel_is_rejected
```

Current audited open-channel theorem:

```text
current_beyond_qm_correction_channel_is_audited
```

This gives the working split:

```text
standard_QM_sector_projection:
  recover the finite Born/Hilbert sector where its witness assumptions hold.

fundamental_IDT_dynamics:
  lower primitive/source dynamics still under investigation.

audited_correction_terms:
  possible deviations from ideal QM only when fixed by a declared lower source,
  boundary, observable, and no-postfit holdout protocol.
```

So the research direction is not to adjust primitives until QM is matched
perfectly. The direction is to keep QM as the high-precision sector constraint
and look for lower-source correction channels that survive audit.

## 175.26. Born/Hilbert Sector-Correction Classifier

The correction protocol now has an executable classifier:

```text
scripts/evaluate_born_hilbert_sector_corrections.py
```

Current verdict:

```text
BORN_HILBERT_SECTOR_CORRECTION_AUDIT_PASS
```

The classifier keeps exactly one channel type open:

```text
audited_lower_source_scale_channel:
  standard sector fixed;
  lower primitive source declared;
  scale/context boundary declared;
  observable declared;
  no-postfit retuning;
  holdout comparison required;
  no Born/Hilbert redefinition;
  reduces to standard QM inside boundary;
  null correction allowed.
```

It rejects:

```text
free_fit_residual;
born_rule_redefinition;
hilbert_carrier_override;
nonzero_inside_standard_boundary;
no_holdout_channel.
```

This gives a concrete working rule for Born/Hilbert:

```text
Do not fit the primitives to Born/Hilbert.
Use Born/Hilbert as the fixed standard-sector baseline.
Only lower-source, predeclared, holdout-tested correction channels may go
beyond that baseline.
```

## 175.27. Full Finite Standard-QM Sector Closure Pass

The broad Born/Hilbert/tensor/unitary pass now has one machine-checked sector
closure artifact:

```text
Proofs/QMClosure/FullQMSectorClosure.lean
```

The theorem is:

```text
b1_context_first_frontier_closes_full_finite_standard_qm_sector
```

It composes, in one Lean proof:

```text
B1 CGSC clause derivation;
context-first constructive witness completeness;
carrier-frontier exhaustion;
finite Born/Hilbert route closure;
Hilbert frontier closure;
readout, dynamics, composite, monoidal, projective-limit, and calibrated-scale
semantic scaffolds;
no Born/Hilbert/unitary/tensor/Stone/spectral imports.
```

Executable status:

```text
scripts/evaluate_full_qm_sector_closure.py

full_qm_sector_closure =
  FULL_FINITE_STANDARD_QM_SECTOR_CLOSED_CONDITIONAL

finite_sector_closed = true
exact_fundamental_qm_closed = false
target_open = 0
target_failed = 0
target_imported = 0
exact_frontier_obligations = 6
external_target_adequacy_obligations = 4
```

The important status correction is:

```text
missing_clause_proofs = 0
conditional_proof_artifacts = 20
```

So the current blocker is no longer "CGSC clauses missing". The blocker is:

```text
derive B1 / context-first constructive witness completeness /
carrier-frontier exhaustion from the lower base;
promote conditional Hilbert/Born/unitary/tensor adequacy artifacts to formal
primitive proofs;
derive exact universal Born readout for all admissible contexts;
keep hbar_I calibrated unless a first-principles scale theorem is supplied.
```

This is not a proof of exact fundamental QM. It is the strongest checked sector
result so far: the finite standard-QM sector closes under the stated successor
base and frontier assumptions, and the remaining wall is sharply localized.

The exact closure contract is now explicit in Lean:

```text
ExactFundamentalQMClosureContract
full_finite_sector_plus_exact_contract_closes_exact_fundamental_qm
```

and each missing component has a blocking theorem:

```text
missing_lower_base_b1_blocks_exact_fundamental_qm
missing_context_first_witness_completeness_blocks_exact_fundamental_qm
missing_carrier_frontier_exhaustion_blocks_exact_fundamental_qm
missing_external_target_adequacy_blocks_exact_fundamental_qm
missing_exact_universal_born_blocks_exact_fundamental_qm
missing_first_principles_phase_scale_blocks_exact_fundamental_qm
```

The external target adequacy wall is also split into four independent theorem
obligations:

```text
ExternalQMAdequacyTheorems
ExternalHilbertBornUnitaryTensorAdequacyClosed
external_qm_adequacy_theorems_close_target_adequacy
current_external_qm_adequacy_theorems_not_closed
```

Those four obligations are:

```text
hilbertRepresentationAdequacy;
bornReadoutAdequacy;
unitaryDynamicsAdequacy;
tensorCompositeAdequacy.
```

## 175.28. Exact Universal Born Contract

The Born wall is now split cleanly:

```text
finite direct Born route: closed
exact universal Born readout: not closed
```

The new Lean object is:

```text
UniversalBornReadoutContract
ExactUniversalBornReadoutClosed
universal_born_readout_contract_closes_exact_universal_born
current_born_frontier_has_finite_route_but_not_universal_born
```

The exact universal Born contract requires:

```text
finiteDirectBornRoute;
allAdmissibleContextsCloseFiniteBornChain;
universalSignedReadout;
universalConstructorRespectingBranchLabels;
universalPhaseDoubleCover;
noPrimitiveHigherOrderFacticization;
probabilityAccountingIsOnlyReadout;
noBornImport.
```

The important correction is that probability accounting is not a missing
selector anymore. It is explicitly downstream of the selected weights. The
remaining Born wall is the universalization of the oriented finite route:

```text
all admissible contexts;
universal signed readout;
constructor-respecting branch labels;
phase double cover;
no primitive higher-order facticization.
```

The updated proof search reports:

```text
finite_direct_born_hits = 2
OPEN_CORE =
  all_admissible_contexts_close_finite_born_chain,
  universal_signed_readout,
  universal_constructor_respecting_branch_labels,
  universal_phase_double_cover,
  no_primitive_higher_order_facticization,
  derive_compatible_kernel_additivity_from_primitives,
  derive_normalized_overlap_selector_from_primitives
```

So Born is not "unknown" anymore in a broad sense. The finite readout algebra
is closed, and the exact universal frontier is now a finite list of primitive
obligations.

## 175.29. Schrodinger As Generator-Readout Dynamics

The dynamics question is now separated from the physical action-scale question.
In IDT terms, the Schrodinger layer is not an outcome readout. It is a
generator readout of reversible inherited distinguishability before outcome
actualization.

The action-scale-free target is:

```text
phase orientation
+ normalized transition readout
+ reversible D_cl automorphism
+ overlap-preserving projective action
+ continuous identity-connected inheritance flow
+ closed frequency generator
+ no target dynamics import
=> frequency-form generator readout
```

The intended projection is the frequency form:

```text
i d_t psi = Omega psi
```

This is not the physical energy-form equation. The latter remains outside this
artifact.

New machine-checkable artifact:

```text
Proofs/QMClosure/SchrodingerGeneratorLogic.lean
```

The central theorem is:

```text
package_and_boundary_close_frequency_schrodinger_logic
```

It shows that a CGSC package plus a primitive boundary supplying normalized
overlap and phase orientation closes the action-scale-free frequency-generator
logic.

The executable probe is:

```text
scripts/evaluate_schrodinger_logic_attempt.py
```

It distinguishes:

```text
frequency_generator_readout: hit
finite_update_gates_only: open
projective_overlap_symmetry_without_continuity: open
continuous_flow_without_generator: open
generator_without_phase_orientation: open
energy_form_shortcut: rejected
imported_schrodinger_equation: rejected import
```

Current conclusion: Schrodinger is best treated as a dynamics readout:
the generator of stable reversible distinguishability transport, represented in
the oriented phase carrier. This links it directly to the same normalized
overlap / phase-orientation machinery that supports the Born and Hilbert
routes, without upgrading the physical scale claim.

## 175.30. Existing QM Logic Connectedness Audit

The current audit scope is deliberately narrower than full QM. It checks only
the nodes already available in the finite/conditional package:

```text
Bell angle/correlation screen;
finite Born readout;
Hilbert phase-bundle carrier route;
Schrodinger frequency-generator dynamics readout.
```

It does not check the open obligations:

```text
universal Born;
universal Hilbert uniqueness;
exact fundamental QM;
first-principles action scale.
```

The executable audit is:

```text
scripts/evaluate_qm_existing_logic_connectedness.py
```

Current result:

```text
qm_existing_logic_connectedness =
  EXISTING_QM_LOGIC_CONNECTED_CONDITIONAL
```

The shared anchors are:

```text
normalized_oriented_distinguishability;
phase_orientation;
normalized_transition_readout;
phase_bundle_square.
```

No direct contradiction was found between the existing Bell, Born, Hilbert, and
Schrodinger nodes. The important boundary tension is:

```text
unitary_generator_reconstruction_demo contains an hbar/Hamiltonian sample.
```

This is not currently a logical contradiction because:

```text
hbar_I = blocked;
qm_generator_translation_closure_I = target;
full_QM_I = target;
schrodinger_frequency_generator_readout_demo is action-scale-free and has no
target-equation import.
```

Therefore the tension is quarantined: the legacy Hamiltonian-scale sample may
remain as a calibrated/target-side demonstration, but it must not be used as
evidence for the action-scale-free Schrodinger readout or for exact full QM.

## 175.31. Universal Born/Hilbert Frontier Closure Contract

The Born/Hilbert wall has been narrowed to one combined frontier contract.
The finite route is no longer the central uncertainty: finite Born readout,
finite phase-bundle/Hilbert carrier screens, Bell angle screens, and the
Schrodinger frequency-generator readout are connected. The remaining question
is the universal quantifier over all admissible contexts and carriers.

The new Lean artifact is:

```text
Proofs/QMClosure/BornHilbertUniversalClosure.lean
```

It proves the conditional closure:

```text
context-first universal endpoint data
+ carrier-frontier exhaustion
=> exact universal Born readout
+ frontier-scoped Hilbert representation
```

The executable evaluator is:

```text
scripts/evaluate_universal_born_hilbert_frontier.py
```

Current result:

```text
universal_born_hilbert_frontier =
  UNIVERSAL_BORN_HILBERT_FRONTIER_CLOSED_CONDITIONAL
pairwise_endpoint_core =
  PRIMITIVE_PAIRWISE_ENDPOINT_COVERAGE_TO_BORN_PROVED
primitive_boundary_endpoint_core =
  PRIMITIVE_BOUNDARY_ENDPOINT_COVERAGE_TO_BORN_PROVED
context_first_endpoint_core =
  CONTEXT_FIRST_ENDPOINT_DATA_TO_BORN_HILBERT_PROVED
current_base_coverage =
  CURRENT_BASE_DOES_NOT_FORCE_PAIRWISE_ENDPOINT_COVERAGE
b1_endpoint_data =
  B1_INTERFACE_DOES_NOT_FORCE_CONTEXT_FIRST_ENDPOINT_DATA
exact_from_b0_closed = False
```

This is a real narrowing of the wall, not a full-QM upgrade. It says that the
Born exponent/readout selector and Hilbert carrier route close together once
the lower layer supplies:

```text
derive_context_first_universal_endpoint_data_from_B0_or_successor_base;
prove_carrier_frontier_exhaustion_over_all_admissible_carriers.
```

The Born-side meaning is precise. Probability accounting does not select Born:
the existing Born-wall separator shows that accounting admits non-quadratic
stable readouts. Born is selected only after the source is constrained to a
primitive pairwise endpoint coverage law. The new Lean bridge proves that this
supplies endpoint-stable binary oriented contexts and blocks primitive ternary
facticization before the Born readout is selected.

The new theorem names are:

```text
primitive_pairwise_endpoint_coverage_blocks_ternary_witness
primitive_pairwise_endpoint_coverage_selects_born
current_primitive_discipline_does_not_force_primitive_pairwise_endpoint_coverage
primitive_boundary_pairwise_closure_supplies_pairwise_basis
primitive_boundary_endpoint_coverage_selects_born
context_first_endpoint_data_selects_born
context_first_endpoint_data_plus_frontier_closes_born_hilbert
b1_interface_projection_does_not_force_context_first_endpoint_data
```

The current negative side is also machine-checked: `S2BornProofSearch.lean`
still admits a local ternary witness under the present primitive discipline.
Therefore universal Born must not be marked as proved until context-first
universal endpoint data is derived below the QM-sector readout.

The Hilbert-side meaning is also precise. The finite complex phase-bundle route
survives the current carrier screens, and the known non-complex samples are
rejected. Universal Hilbert representation still needs carrier-frontier
exhaustion; finite samples alone are not a universal quantifier over carriers.

Forbidden upgrades:

```text
does_not_prove_full_QM_I;
does_not_prove_universal_Born_from_B0_alone;
does_not_prove_universal_Hilbert_uniqueness_from_finite_frontier_alone;
does_not_derive_hbar_I.
```
