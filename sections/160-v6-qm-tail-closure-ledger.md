## 160. v6 QM Tail Closure Ledger

Status:

`v6_qm_tail_closure_ledger_initialized`

This section closes the bookkeeping ambiguity left by the older QM sections.

There are two different claims:

1. first-principles full QM;
2. calibrated v6 QM reconstruction.

The first remains open.

The second is now conditionally closed.

### 160.1. First-principles spine

The first-principles target remains:

`full_QM_I = target`.

Reason:

`hbar_I = blocked`

and the primitive action standard is not derived.

The following are therefore not accepted:

1. `full_QM_I = derived`;
2. `hbar_I = derived`;
3. `qm_dynamics_action_facticity_closure_I = derived`.

Status:

`first_principles_QM_remains_open`

### 160.2. Calibrated v6 spine

The calibrated target is:

`calibrated_QM_reconstruction_I`.

Its accepted status is:

`derived_conditional`.

The condition is explicit:

`calibrated_hbar_I = bridge_assumption`.

No hidden \(\hbar_I\) derivation is claimed.

Status:

`calibrated_QM_reconstruction_closed_conditionally`

### 160.3. Tail table

| Tail | v6 calibrated status | First-principles status |
|---|---|---|
| finite Born/context readout | closed conditionally | conditional |
| finite unitary/context networks | closed conditionally | conditional |
| continuum frequency generator | `calibrated_qm_continuum_closure_I = derived_conditional` | primitive generic proof open |
| Hamiltonian scale | `calibrated_hamiltonian_energy_operator_I = derived_conditional` | `hbar_I` blocked |
| momentum / de Broglie | `calibrated_momentum_operator_I`, `calibrated_de_broglie_relation_I = derived_conditional` | `hbar_I` blocked |
| canonical sector | `calibrated_canonical_commutator_I = derived_conditional` | exact finite commutator impossible; continuum first-principles open |
| apparatus context | `calibrated_qm_apparatus_facticity_closure_I = derived_conditional` | primitive macroscopic apparatus construction open |
| facticity | `measurement_facticity_I = derived_conditional` | quantitative irreversible scaling law open |

Status:

`QM_tail_table_registered`

### 160.4. Machine status

The v6 verifier now checks:

1. `full_QM_I` cannot be claimed as derived prematurely;
2. `qm_dynamics_action_facticity_closure_I` cannot be claimed as derived
   while first-principles action/facticity nodes are not closed;
3. `calibrated_QM_reconstruction_I` needs an explicit calibrated route;
4. `calibrated_QM_reconstruction_I = derived` is rejected while any calibrated
   required node remains open or target.

Therefore the calibrated branch may be used as a completed reconstruction
spine, while the stronger first-principles branch remains honestly open.

Status:

`v6_QM_machine_status_closed`

### 160.5. Remaining non-QM work

The remaining work is no longer a QM-tail problem.

It is one of:

1. universe-sector selection;
2. first-principles action standard;
3. primitive macroscopic apparatus construction;
4. non-standard residual prediction.

These are outside the calibrated v6 QM closure.

Status:

`QM_tails_closed_under_calibrated_v6_scope`
